# 评估工具统筹与交接说明

> 2026-08-14 由人员2 整理，供全组阅读。本文说明人员4（宋小平）交付的"统一评估指标库"如何与 `基础U-net.ipynb` 对接、当前指标口径与后续交接点。

## 1. 背景：为什么需要这套评估工具

- 申报书要求对分割结果做量化评估：**Dice、IoU、像素准确率、肿瘤平均熵（不确定性）、拒绝率、覆盖率、选择性准确率**，以及统计检验（Pearson 相关、配对 t 检验、ECE）。
- 人员4 交付了统一指标库 `src/metrics/unified_metrics.py`，把上述指标收敛为**一个函数入口** `calculate_all_metrics(...)`，团队所有人调用同一套代码，避免"各算各的、口径不一致"。
- 已接入 `基础U-net.ipynb` 作为 **模块12**：训练完成后直接得到指标表格，并导出 CSV。

## 2. 交付文件与职责

| 文件 | 内容 | 谁用 |
|---|---|---|
| `src/metrics/unified_metrics.py` | 统一指标库：Dice / IoU / 像素准确率 / 熵 / 拒绝率 / 覆盖率 / 选择性准确率 / ECE / Pearson / 配对 t 检验 / 读 npz 工具 | 所有人 |
| `test_metrics.py` | 独立自测脚本：用随机数据验证整套指标可运行 | 人员4 维护，其他人可跑 |
| `requirements.txt` | 依赖清单 | 所有人 |
| `基础U-net.ipynb` 模块12 | 评估单元：推理测试集 → 计算指标 → 导出 CSV | 所有人（已就绪） |

## 3. 怎么跑（VSCode 本地）

1. 拉取仓库最新代码（`git pull`）。
2. 本机无需虚拟环境，用 `E:\python` 解释器运行 notebook；依赖已齐全（numpy / torch / scipy / sklearn / matplotlib）。
3. 依次运行 模块0 → 模块12；模块12 在训练与推理之后执行，输出：
   - 终端打印 7 项指标；
   - `output/evaluation_metrics.csv`（标准表格，供论文 / 结题）。
4. 只想自测指标库本身：终端运行 `python test_metrics.py`。

## 4. 指标口径（重要，防止误读）

- **二值口径**：`calculate_all_metrics` 只接受 0/1 二值掩膜。本模型输出 3 类（0 背景 / 1 肾脏 / 2 肿瘤），模块12 已把**肿瘤类**（`pred==2`）单独二值化再评估；要评估肾脏，把 `== 2` 改成 `== 1`。
- **空掩膜规则**：预测与标签都为空 → Dice=1、IoU=1（视为完美）；一边为空一边非空 → 0。CT 切片里肿瘤通常只占少数，很多切片"双空"，所以 Dice 与 IoU 数值会非常接近——这是正常现象，不是 bug。
- **占位值（重要）**：人员3 的 `mc_predict`（贝叶斯不确定性输出）尚未交付，模块12 目前给熵图传全 0，因此：
  - `tumor_mean_entropy=0`、`rejection_rate=0`、`coverage=1`、`selective_accuracy=dice` **是无信息的占位结果，不能写进论文 / 结题**；
  - `dice / iou / pixel_acc` 是真实计算结果。
- **像素准确率会被背景主导**（肿瘤占比很小），分数接近 1 不代表分割好，务必以 Dice / IoU 为准。

## 5. 分工与交接点（统筹速览）

| 人员 | 职责 | 与评估工具的关系 |
|---|---|---|
| 人员1 / 人员5 | 模型训练、论文撰写 | 读 `output/evaluation_metrics.csv`，用统一指标出图 / 出表 |
| 人员2  | 数据、小样本、本地验证 | 维护 `kits19_small`，保证本地能跑通模块12 |
| 人员3  | 贝叶斯管线 `mc_predict` | **待交付真实熵图**（契约 A），替换模块12 占位 |
| 人员4  | 指标库 + 统计检验 | 维护 `unified_metrics.py`；拿到多人 / 多模型结果后做 Pearson / 配对 t 检验 |

**契约 A（人员3 → 全组）**：`mc_predict` 输出格式约定为 `mean / variance / entropy` 三个通道的 npz + `meta.json`（含病例、切片、类别说明）。交付后，模块12 只需把 `entropy_map` 占位换成真实熵图，熵相关指标即变为真实值。

## 6. 待办与注意事项

- `requirements.txt` 缺少 `nibabel`（notebook 读 .nii.gz 需要，指标库本身不需要）——请人员4 补充 `nibabel>=5.0`。
- 本地 1 epoch 的调试数字**不代表真实性能**，正式结论以 Kaggle 全量训练为准。
- 任何成员修改 `unified_metrics.py` 的函数签名前，先跑 `python test_metrics.py` 自测，并通知人员4。
